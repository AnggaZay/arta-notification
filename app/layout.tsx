import "./globals.css";
import Providers from "./Providers"; // Impor file yang lu buat tadi

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}